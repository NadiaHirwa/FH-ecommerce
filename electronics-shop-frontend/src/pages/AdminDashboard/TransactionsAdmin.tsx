import React from 'react';
import './AdminDashboardLayout.css';

export const TransactionsAdmin: React.FC = () => (
  <div>
    <h2>Transactions (Unified)</h2>
    <p>All transactions (online + offline), filters, edit/refund actions. (placeholder)</p>
  </div>
);
